#include <bits/stdc++.h>
using namespace std;
int main()
{
    // string s = "First way to declare an object with string constructor";
    // string s("Second way to declare an object with string constructor");
    // string s("Third way to declare an object with string constructor", 10);
    string a = "hello";
    // string s(a, 2); // to declare an object with .... ans is "llo"
    string s("Hello", 2); // "He"
    // string s(2, 'x');  // xx
    cout << s << endl;
    return 0;
}